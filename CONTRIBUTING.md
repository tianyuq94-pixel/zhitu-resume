# 参与贡献

感谢你愿意改进职途简历。

## 提交 Issue

- 先确认问题在最新版仍可复现。
- 描述操作步骤、预期结果和实际结果。
- 可以提供脱敏截图，但不要上传真实简历、密码、Cookie、数据库地址或 API Key。

## 提交 Pull Request

1. 从 `main` 创建功能分支。
2. 保持改动范围清晰，并补充必要的测试。
3. 后端运行 `python -m pytest`。
4. 前端运行 `pnpm build`。
5. 在 Pull Request 中说明改动目的、验证方式和界面变化。

## 开发原则

- AI 不得凭空补充用户没有提供的经历、技能或数据。
- 用户上传文件与生成结果必须保持私有。
- 新增环境变量时同步更新示例文件，但不得提交真实值。
- 面向用户的文案不暴露模型供应商、内部提示词或错误堆栈。
